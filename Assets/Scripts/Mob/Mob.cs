using UnityEngine;
using System.Collections;

public class Mob : MonoBehaviour {

    enum typeMob { boss, model, squad, teacher, politic};
    typeMob t;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
